server:
  port: 9000
  servlet:
    session:
      timeout: 10m
logging:
  level:
    org.springframework.security: trace
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/auth2_authorization_server
    username: local_dev
    password: local_dev
  jpa:
    show-sql: true